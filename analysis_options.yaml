include: package:flutter_lints/flutter.yaml

analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true

  plugins:
    - custom_lint

  exclude:
    # Generado por build_runner: se regenera, no se revisa a mano.
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "**/generated_plugin_registrant.dart"

  errors:
    # R5: el gate corre con --fatal-infos --fatal-warnings. Estas ademas
    # se elevan a error para que fallen tambien fuera del gate.

    # RNF-06: un print puede filtrar token, cedula, diagnostico o receta.
    avoid_print: error

    # Un TODO sin ticket es deuda invisible (REVIEW_GATE 3.6).
    todo: warning

    # Deuda que en este proyecto duele especialmente.
    always_use_package_imports: error
    avoid_dynamic_calls: error
    close_sinks: error
    cancel_subscriptions: error

linter:
  rules:
    # ── Correccion ────────────────────────────────────────────────────
    always_declare_return_types: true
    avoid_slow_async_io: true
    avoid_type_to_string: true
    collection_methods_unrelated_type: true
    comment_references: true
    literal_only_boolean_expressions: true
    no_adjacent_strings_in_list: true
    throw_in_finally: true
    unnecessary_statements: true
    test_types_in_equals: true

    # ── Estilo e intencion ────────────────────────────────────────────
    prefer_final_locals: true
    prefer_final_in_for_each: true
    prefer_single_quotes: true
    require_trailing_commas: true
    sort_constructors_first: true
    unawaited_futures: true
    use_enums: true
    use_super_parameters: true
    directives_ordering: true

    # ── Rendimiento en UI (F13 / RNF) ─────────────────────────────────
    prefer_const_constructors: true
    prefer_const_constructors_in_immutables: true
    prefer_const_declarations: true
    prefer_const_literals_to_create_immutables: true
    sized_box_for_whitespace: true
    use_colored_box: true
    use_decorated_box: true

    # ── Accesibilidad y diseño ────────────────────────────────────────
    use_build_context_synchronously: true
