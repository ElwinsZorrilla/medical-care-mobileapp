# Tags declarados del proyecto.

# Para correrlas contra el backend levantado hace falta anular la exclusion,
# no solo pedir el tag: `--tags` no gana sobre `exclude_tags`. Este preset lo
# hace en un comando:
#
#   cd back && npm run start:dev
#   cd front && flutter test -P servidor
